[user]
    name = Yudai NAKATA
    email = yudai-nkt@users.noreply.github.com
[color]
    ui = auto
[core]
    excludesfile = ~/.gitignore_global
    autocrlf = input
[alias]
    graph = log --graph --pretty=format:'%C(red)%h%C(reset) -%C(yellow)%d%C(reset) %s %C(green)(%cr)%C(reset)' --date-order
    st = status -sb
    cmt = commit -m
    rm-idx = rm --cached
